import 'package:crocodile_game/app/model/models.dart';
import 'package:flutter/material.dart';

class SnackbarMessage {
  final String text;
  final Color color;

  SnackbarMessage({this.text, this.color});
}